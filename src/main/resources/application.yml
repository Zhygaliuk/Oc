spring:
  datasource:
    url: jdbc:mysql://localhost:3306/courework?allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: pavilion1219
    driver-class-name: com.mysql.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy

server:
  port: 8082
